# v6.5.0 System Resilience Audit

## Checks
- Vite/TypeScript/PixiJS 8/Howler/Firebase dependencies retained.
- WebGL detection added before Pixi stage initialization.
- HTML fallback fishing loop added for WebGL or asset failure.
- PWA cache bumped to v6.5.0.
- New regions are linked to unlock logic, fish pool, dex, service worker, and validation.
- Runtime maintains no legacy HUD/debug labels.

## Risk notes
- Browser fullscreen and orientation lock still depend on user gesture/PWA install policy. The game fails silently and keeps layout stable.
- Large art expansion remains PNG/WebP hybrid; future build can move more UI icons into packed atlas frames.
