# v6.7.0 Portrait Rescue Audit

## 확인 항목
- `manifest.webmanifest` orientation: `portrait-primary`
- `src/core/PortraitGuard.ts` 추가
- `screen.orientation.lock('portrait-primary')` 시도
- 실패 시 CSS portrait shell 유지
- 카카오톡 인앱 브라우저 감지 클래스 지원
- landscape media query가 가로 UI로 전환하지 않도록 후순위 CSS에서 차단
- 로그인 화면에서 낚시 HUD / 상점 / 버전 배지 숨김 유지
- 캐시 버전 v6.7.0 갱신
